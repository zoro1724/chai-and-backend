# chai aur backend

this is new journey stared and idk

# we create .gitkeep file for that temp folder will push
# 2nd step we create gitignore file to keep sensitive file togather to keep item that we want to ignore like any information by gitignore generator(and serach there (NODE) and keep all sensitive file of node.) 
# next thing to make .env file and then .env.sample file
# create src(source) folder 
# after cretating file in src write command "type":"module",in package.json ans we want importing from this method.(two ways of importing :- 1:-common js and 2:-importing)
# now we use nodemon once file will save it restart the file means relode automatically.
# after installing it by npm -i D nodemon (as it only use while in development so (dev dependency) and will not go to production.)
# "dev":"nodemon src/index.js" in script tag of package.json
# next step to create folder like controllers,utils,middlewares,models,routes,db in src
# now we can also use (prettier ) for working in a team /as it tells other member what standard everyone will use.
  ## means team leader will decide how much spacing and all other thing to avoid the conflict between developer. 


# git add .
# git commit -m "message"
# git push
# hello world
# git init
# git add .
# git commit -m "message"
# git branch -M main
# git push -u origin main
# node- module folder created when we install (nodemon).
